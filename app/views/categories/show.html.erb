<p id="notice"><%= notice %></p>

<p>
  <strong>Name</strong>
  <%= @category.name %>
  <% @category.transactions.each do |transaction| %> 
    <%= transaction.amount %>
  <% end %>
</p>

<%= link_to 'Back', categories_path %>